apiVersion: v1
kind: Secret
metadata:
  name: esddns-gandi-credentials
  namespace: esddns-system
  labels:
    app: esddns
type: Opaque
stringData:
  # IMPORTANT: Replace with your actual Gandi.net API key
  # This should be created separately via kubectl or sealed-secrets for production
  api-key: "INSERT_YOUR_GANDI_API_KEY_HERE"
